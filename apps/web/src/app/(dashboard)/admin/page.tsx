// Placeholder de la ruta /admin — se implementa en Fase 5 del plan de reescritura,
// consumiendo la API de apps/api y reusando features/admin.
export default function Page() {
  return <div>admin (placeholder — Fase 5)</div>;
}
