// Tab "ventas" de /analytics — mismo componente de feature que la ruta standalone
// equivalente (features/ventas), montado acá como tab. Placeholder (Fase 5).
export default function Page() {
  return <div>Analytics / ventas (placeholder — Fase 5)</div>;
}
