// Tab "insights" de /analytics — mismo componente de feature que la ruta standalone
// equivalente (features/insights), montado acá como tab. Placeholder (Fase 5).
export default function Page() {
  return <div>Analytics / insights (placeholder — Fase 5)</div>;
}
